import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'upkeep-frontend',
  webDir: 'www/browser',
};

export default config;
